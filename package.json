{
  "name": "hector-mcp-plugin-test",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal MCP server to verify connectivity from Claude and ChatGPT",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "^4.1.13"
  }
}
